#include <stdio.h>

/**
 * main - prints digits
 *
 * Description: prints all digits from 0 to 9
 *
 * Return: 0 always success
*/

int main(void)
{
	int u = '0';

	while (u <= '0')
	{
		putchar(u);
		u++;
	}
	putchar('\n');
	return (0);
}

