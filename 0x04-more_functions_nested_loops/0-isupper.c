#include <stdio.h>

/**
 * main - check the code.
 *
 * Description: checking if c is uppercase
 *
 * Return: Always 0.
 */

int _isupper(int c);
{
if (c >= 65 && c <= 90)
{
	c = 1;
}
	else
	{
		c = 0;
	}
}
