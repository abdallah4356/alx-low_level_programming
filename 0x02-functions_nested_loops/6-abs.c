#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @int: the integer itself
 *
 * Return: -int if it's negative
*/

int _abs(int r)
{
	if (r < 0)
	return (-r);

	else
	return (r);
}
